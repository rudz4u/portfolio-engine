/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Do NOT set output: 'standalone' — @netlify/plugin-nextjs handles deployment output
}

module.exports = nextConfig
